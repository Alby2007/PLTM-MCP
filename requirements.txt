# Core dependencies for PLTM MCP Server
mcp>=1.0.0
aiosqlite>=0.19.0
loguru>=0.7.0
numpy>=1.24.0
arxiv>=2.0.0

# Optional: For LLM-based extraction (not required for core functionality)
# openai>=1.0.0
